== README

This is a simple and clean and versioned API made with Rails::API.

There is only one table of "tasks" containing "name" and "deadline". 

Install:
<tt> git clone https://github.com/jmrezayi2/rubyback.git</tt>

You need to run <tt> rake db:migrate </tt> for the first time to run it.

